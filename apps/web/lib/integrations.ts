// Deprecated. The public site no longer surfaces editor integrations — bloxscout
// is a hosted AI agent, not an installable tool. Kept as an empty module so any
// stray import fails loudly rather than silently resolving stale content.
export {};
